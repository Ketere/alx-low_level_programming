#include <unistd.h>

/**
 *_putchar - wites the character c to stdout
 *gc: The character to print
 *Return on success 1.
 *On error, -1 is returned, abd errand is set appropriately.
 */

int _putchar(char c)
{
	return (write(1, &c, 1));
}